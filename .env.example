VITE_API_URL=http://localhost:4000
# No map API key needed — the map preview uses Leaflet + OpenStreetMap tiles, and address
# search uses the backend's Nominatim proxy (PLAN.md "Картографический стек").
